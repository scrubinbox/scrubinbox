# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Environment variables
.env
.env.local
.env.*.local
*.local

# Wrangler / Cloudflare local state
.dev.vars
.wrangler/

# Credentials (should never be committed)
credentials.json
token.json
data/
client_secret_*.json

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*~

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Test coverage
coverage/

# OpenTofu / Terraform configuration moved to the private scrubinbox-infra repo.

# Personal/operational notes that must not be published.
productization.md

# CASA prep documents (self-assessment, architecture diagrams, retention
# policy, scope justification, etc). These describe our internal security
# posture in detail for the assessor and would give attackers a map of our
# defense surface if published. Kept locally in this directory + shared
# directly with TAC Security.
docs/casa/

# Claude Code local state (settings, memory, subagents, plans)
.claude/
