# Frontend env (loaded by Vite). Copy to .env (gitignored) and fill in.
#
# Staging values: Supabase dashboard for project jjbychixggcefuxznprp →
# Project Settings → API. Use the new-format `sb_publishable_...` key here
# (Supabase's legacy `anon` JWT is being deprecated).
VITE_SUPABASE_URL=https://jjbychixggcefuxznprp.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=
